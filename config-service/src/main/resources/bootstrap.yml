server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LuisMSotamba/orchestation.git
          default-label: main
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: kalkstein
      password: th3_eth3r
